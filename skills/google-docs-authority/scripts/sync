#!/bin/sh
set -eu
script_dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
export PYTHONPATH="$script_dir/../src${PYTHONPATH:+:$PYTHONPATH}"
exec "${GOOGLE_DOCS_AUTHORITY_PYTHON:-python3}" -B -m google_docs_authority.mirror "$@"
