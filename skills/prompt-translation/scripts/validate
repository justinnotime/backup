#!/bin/sh
set -eu
script_dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
export PYTHONPATH="$script_dir/../src${PYTHONPATH:+:$PYTHONPATH}"
exec "${PROMPT_TRANSLATION_PYTHON:-python3}" -B -m prompt_translation.validation "$@"
