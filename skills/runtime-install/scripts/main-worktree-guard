#!/usr/bin/env python3
"""Reject selected commits in the explicitly configured shared checkout."""
import sys
from pathlib import Path

sys.dont_write_bytecode = True
sys.path.insert(0, str(Path(__file__).resolve().parents[1] / "src"))
from runtime_install.git_hooks import main_guard

raise SystemExit(main_guard())
