#!/bin/sh
set -eu
PACKAGE=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
PYTHONPATH="$PACKAGE/src${PYTHONPATH:+:$PYTHONPATH}" exec "${RUNTIME_INSTALL_PYTHON:-python3}" -B -c 'import sys; from runtime_install.install import main; raise SystemExit(main("cron", sys.argv[1:]))' "$@"
